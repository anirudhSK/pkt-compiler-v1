To build (Steps tested on Ubuntu 14.10):

sudo apt-get update
sudo apt-get install clang llvm-dev libclang-dev autotools-dev dh-autoreconf git
git clone https://github.com/anirudhsk/jayhawk
cd jayhawk
./autogen.sh
./configure
make

To build clang tools, use the clang.sh script.
